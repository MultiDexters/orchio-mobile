# Assets

These are generated placeholder icons/splash so the project builds out of the box.
Replace them with your real brand art before shipping:

- `icon.png` — 1024×1024
- `adaptive-icon.png` — 1024×1024 (Android foreground)
- `splash.png` — splash logo (shown via `resizeMode: contain`)
- `favicon.png` — web favicon
